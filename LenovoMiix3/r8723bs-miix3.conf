# Lenovo MIIX 3-1030 RTL8723BS workaround
# Use CMD52 only for narrow 32-bit register writes. Keep bulk data on CMD53.
options r8723bs force_cmd52_write32=1

# Clear the card's high-speed bit before Realtek I/O and use legacy 25 MHz timing.
options r8723bs force_sdio_legacy=1
